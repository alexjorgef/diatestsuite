apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diadata-system.fullname" . }}-test-connection"
  labels:
    {{- include "diadata-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diadata-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
