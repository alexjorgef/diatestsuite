# diatestsuite

* dumper: Dump a snapshot of relational data from an existing PostgreSQL database and push it to a Docker registry
* tester: Test environment DIA platform

## Requirements

* bash
* docker