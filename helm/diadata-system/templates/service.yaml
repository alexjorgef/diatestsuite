apiVersion: v1
kind: Service
metadata:
  name: {{ include "diadata-system.fullname" . }}
  labels:
    {{- include "diadata-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diadata-system.selectorLabels" . | nindent 4 }}
