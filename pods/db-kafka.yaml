apiVersion: "v1"
kind: Pod
metadata:
  name: "diadata-clusterdev-kafka"
spec:
  containers:
  - name: clusterdev-kafka
    image: diadata-clusterdev-kafka:0.1
    env:
    - name: KAFKA_ZOOKEEPER_CONNECT
      value: "localhost:2181"
    - name: KAFKA_ADVERTISED_LISTENERS
      value: "PLAINTEXT://localhost:9092"